name: PHP CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # 明日からのGitHub強制仕様に先回り対応

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, pdo, pdo_pgsql # pdo_postgresqlからpdo_pgsqlに修正

    - name: Check PHP Syntax (Lint)
      run: |
        find . -name "*.php" -not -path "./vendor/*" | xargs -n 1 php -l
